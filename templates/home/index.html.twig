{% extends 'base.html.twig' %}

{% block title %}Accueil - Murim Mania {% endblock %}

{% block body %}
<div class="hero-section">
    <div class="container">
        <h1><i class="fas fa-dragon me-3"></i>Bienvenue dans le Monde du Murim</h1>
        <p class="lead">Découvrez les secrets de la cultivation, les techniques martiales légendaires et les mystères des sectes anciennes</p>
    </div>
</div>

<div class="main-content">
    <div class="container py-5">
        <div class="row">
            <div class="col-lg-8">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2><i class="fas fa-scroll me-2 text-warning"></i>Articles Récents</h2>
                    <a href="{{ path('app_article_index') }}" class="btn btn-outline-primary">
                        Voir tous les articles <i class="fas fa-arrow-right ms-1"></i>
                    </a>
                </div>

                {% if articles is empty %}
                    <div class="text-center py-5">
                        <i class="fas fa-scroll fa-3x text-muted mb-3"></i>
                        <h4 class="text-muted">Aucun article disponible</h4>
                        <p class="text-muted">Les premiers articles du Murim arrivent bientôt...</p>
                        {% if app.user %}
                            <a href="{{ path('app_article_new') }}" class="btn btn-primary mt-3">
                                <i class="fas fa-plus me-1"></i>Créer le premier article
                            </a>
                        {% endif %}
                    </div>
                {% else %}
                    <div class="row">
                        {% for article in articles %}
                            <div class="col-md-6 mb-4">
                                <div class="card h-100">
                                    <div class="card-body">
                                        <div class="d-flex justify-content-between align-items-start mb-2">
                                            <a href="{{ path('app_article_index', {category: article.category.id}) }}" 
                                               class="category-badge" 
                                               style="background-color: {{ article.category.color }}">
                                                {{ article.category.name }}
                                            </a>
                                            <small class="text-muted">
                                                {{ article.createdAt|date('d/m/Y') }}
                                            </small>
                                        </div>
                                        <h5 class="card-title">
                                            <a href="{{ path('app_article_show', {id: article.id}) }}" 
                                               class="text-decoration-none text-dark">
                                                {{ article.title }}
                                            </a>
                                        </h5>
                                        {% if article.excerpt %}
                                            <p class="card-text text-muted">{{ article.excerpt }}</p>
                                        {% endif %}
                                        <div class="d-flex justify-content-between align-items-center">
                                            <small class="text-muted">
                                                <i class="fas fa-user me-1"></i>{{ article.author.username }}
                                            </small>
                                            <a href="{{ path('app_article_show', {id: article.id}) }}" 
                                               class="btn btn-sm btn-outline-primary">
                                                Lire la suite
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>

            <div class="col-lg-4">
                <div class="sidebar">
                    <h5><i class="fas fa-tags me-2 text-warning"></i>Catégories</h5>
                    {% if categories is empty %}
                        <p class="text-muted">Aucune catégorie disponible</p>
                    {% else %}
                        <div class="list-group list-group-flush">
                            {% for category in categories %}
                                <a href="{{ path('app_article_index', {category: category.id}) }}" 
                                   class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                                    <span>
                                        <span class="badge rounded-pill me-2" 
                                              style="background-color: {{ category.color }}; width: 12px; height: 12px;"></span>
                                        {{ category.name }}
                                    </span>
                                    <span class="badge bg-secondary rounded-pill">{{ category.articles|length }}</span>
                                </a>
                            {% endfor %}
                        </div>
                    {% endif %}

                    <div class="mt-4">
                        <h5><i class="fas fa-info-circle me-2 text-info"></i>À propos</h5>
                        <p class="text-muted">
                            Ce blog explore l'univers fascinant du Murim, où les arts martiaux rencontrent 
                            la cultivation spirituelle. Découvrez les techniques ancestrales, les légendes 
                            des maîtres et les secrets des sectes mystérieuses.
                        </p>
                        {% if not app.user %}
                            <div class="text-center">
                                <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm">
                                    <i class="fas fa-user-plus me-1"></i>Rejoignez-nous
                                </a>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}